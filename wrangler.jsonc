{
  // 把这个 name 改成你 Cloudflare Workers 项目里实际的名字，
  // 否则 wrangler deploy 会创建一个新的 Worker。
  "name": "hexo",
  "compatibility_date": "2025-06-19",
  // Hexo 生成的静态文件目录，Cloudflare 直接托管这里的内容
  "assets": {
    "directory": "./public"
  }
}
